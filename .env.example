# src/lib/env.ts 의 스키마와 1:1로 대응합니다.
# 이 파일을 .env.local 로 복사한 뒤 값을 채우세요.

# 클라이언트 번들에 포함됩니다. (NEXT_PUBLIC_ 접두사 필수)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# 환경변수 검증을 건너뛰려면 true 로 설정합니다. (CI/린트 등)
# SKIP_ENV_VALIDATION=false
